class Order_data:
    def __init__(self, order_date, order_time, order_status):
        self.order_date = str(order_date)
        self.order_time = str(order_time)
        self.order_status = str(order_status)
